# Shared local frontend env (web + pwa Vite). Not used by Docker — see root .env.
# Browser needs the VITE_ prefix; INTERNAL_API_KEY is only for the web dev proxy.
VITE_AZURE_TENANT_ID=your-tenant-id
VITE_AZURE_CLIENT_ID=your-client-id
INTERNAL_API_KEY=change-me-long-random-secret
